# apb_rtl
The Advanced Peripheral Bus (APB) is a simple, low-power, low-bandwidth bus used to connect peripherals to a system-on-chip (SoC) master. It supports read and write transfers through a two-phase handshake:
